// since we are not storing the token on the server, we do not need to have another route to log out
// it is to be done on the client side

// storing tokens on the database is a very bad practice as they provide access to protected apis

// also use https to send tokens