//global objects

console in console.log()            //print to console

setTimeout()        // call a function after a delay

clearTimeout()

setInterval()       // repeatedly call  a function after a deplay

clearInterval()


// in browsers, the global scope is window

// in node we have global instead of window

window.console.log();       // won't work

global.console.log();       // will work

