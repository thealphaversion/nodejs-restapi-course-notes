/**
 * Helmet is a thrd party express middleware that helps secure our http requests
 * 
 * const helmet = require('helmet');
 * app.use(helmet());
 * 
 * 
 * Another useful middleware is morgan.
 * It is used to log http requests.
 * 
 * const morgan = require('morgan');
 * app.use(morgan('tiny'));             // tiny is the simplest one
 */