// set environment variables using heroku cli

// run
// heroku config:set vidly_jwtPrivateKey=keyvalue
// this will set our private key

// also
// do
// heroku config:set NODE_ENV=production
// this will set the node environment to production

// to see all environment variables, run
// heroku config