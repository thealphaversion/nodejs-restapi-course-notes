/**
 * Some packages are only required during development
 * like static analysis tools or unit testing tools
 * 
 * These should not go into production
 * 
 * To install a library as a development dependency or dev dependency
 * we simply use a flag --save-dev
 * 
 * ex: npm install jshint --save-dev
 * 
 */